id: run_hello_world
version: "1.0"
specVersion: "0.8"
name: Run Hello World Workflow
description: Workflow to run the Hello World Workflow
start: RunHelloWorld
states:
  - name: RunHelloWorld
    type: operation
    actions:
      - subFlowRef: hello_world
    end: true
